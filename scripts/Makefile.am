MAINTAINERCLEANFILES = Makefile.in

install-exec-local:
	install ./rich-core-pattern $(DESTDIR)/etc/init.d
	install ./rich-core-dumps $(DESTDIR)/etc/init.d
	install ./rich-core-dumper $(DESTDIR)/usr/sbin
